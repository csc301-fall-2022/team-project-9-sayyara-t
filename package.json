{
	"name": "sayyara_app",
	"scripts": {
		"install": "cd client && npm install && cd .. && cd server && npm install",
		"install:all": "npm install && cd client && npm install && cd .. && cd server && npm install",
		"ci:all": "npm ci && cd client && npm ci && cd .. && cd server && npm ci",
		"start:server": "cd server && npm start",
		"start:client": "cd client && npm start",
		"test:all": "jest",
		"test:server:unit": "jest --testPathPattern=./server/tests/unit"
	},
	"devDependencies": {
		"jest": "^29.3.0"
	}
}
